# Firewall Role

## Purpose

Installs and configures the Ubuntu UFW firewall.

## Responsibilities

- Install UFW
- Allow SSH (22)
- Allow HTTP (80)
- Allow HTTPS (443)
- Enable the firewall

## Variables

Currently none.

## Example

```yaml
roles:
  - firewall
```
